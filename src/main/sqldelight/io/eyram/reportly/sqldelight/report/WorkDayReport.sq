

CREATE TABLE report (
  work_week INTEGER NOT NULL,
  work_day INTEGER NOT NULL,
  report TEXT,
  time_on TEXT,
  time_off TEXT,
  PRIMARY KEY (work_week, work_day)
);


selectAll:
SELECT *
FROM report;

commitReport:
INSERT INTO report(
work_week,
work_day,
report,
time_on,
time_off
);